from distutils.core import setup
setup(
  name = 'applepie',
  packages = ['applepie'], # this must be the same as the name above
  version = '0.1',
  description = 'Homemade python library to handle text operations.',
  author = 'Jake Ledoux',
  author_email = 'jaik.exe@gmail.com',
  url = 'https://github.com/NyanJaik/applepie', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['string','text','parsing','operations','yes or no','names'], # arbitrary keywords
  classifiers = [],
)
